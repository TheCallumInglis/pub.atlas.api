meta {
  name: Backfill Geodata
  type: http
  seq: 4
}

post {
  url: {{BASE_URL}}/pubs/geocode-missing?limit=20
  body: none
  auth: inherit
}

params:query {
  limit: 20
}

headers {
  X-API-Key: {{APP_API_KEY}}
}

settings {
  encodeUrl: true
  timeout: 0
}
